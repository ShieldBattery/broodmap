[package]
name = "broodmap-analysis"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
rust-version = "1.95"
description = "Static terrain analysis for StarCraft: Brood War maps"

[dependencies]
broodmap = { version = "0.1.0", path = "../broodmap" }
broodmap-formats = { version = "0.1.0", path = "../broodmap-formats" }
thiserror = "2.0"
